import { DomSlotRender } from "../DomRender";
/**
 * @description dom渲染input
 */
export class DomInputRender extends DomSlotRender {
    constructor(parent, parentDom, slot) {
        super(parent, parentDom, slot);
    }
}
